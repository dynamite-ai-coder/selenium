# Core agent framework (pulls pydantic, openai, cdp-use, ... pinned by upstream)
browser-use==0.13.10

# Cloudflare bypass: patched stealth Firefox driven through a Playwright API.
# The engine binary is downloaded at image build time (see Dockerfile).
invisible-playwright==0.14.0

# SeleniumBase (SeleniumBase Pure CDP Mode browser launcher) is intentionally
# NOT listed here: its dependency pins conflict with browser-use. The Dockerfile
# and scripts/start_browser.sh install/run it from an isolated environment
# (/opt/seleniumbase-venv) instead.

# Web framework / server
fastapi==0.141.1
starlette==1.6.0
uvicorn[standard]==0.52.4
websockets==15.0.1
python-multipart==0.0.32
httpx==0.28.1

# Settings
pydantic-settings==2.15.0
